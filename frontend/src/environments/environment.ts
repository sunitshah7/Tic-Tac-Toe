/**
 * Runtime configuration for the local development setup. The backend listens on 5090 by
 * default (see the API's launchSettings.json); change this one value if you run it elsewhere.
 */
export const environment = {
  apiBaseUrl: 'http://localhost:5090/api',
};
